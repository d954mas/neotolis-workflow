# ntgrill vertical slice

This follows the completed TB-01–TB-11 tracer at
`3db1f4c5e66b02aa1bfd65cf07d7bf84545e786d`; it does not change that plan's scope
or history. Authority: the user's separate ntgrill implementation request.
No commit, push, or release publication is authorized.

## Boundary

`brief-ready` → primary decision interview → explicit confirmation of the
complete understanding → full current BRIEF without Open questions →
`plan-ready`. `.ntworkflow/NTGRILL.md`, `RUNTIME.md`, and SPEC requirement 6
own behavior. No new lifecycle, registry, supporting role, or later skill.

## Owned files and checks

1. Close native disk-resume evidence in disposable consumers. Keep generated
   logs outside this checkout's contract directory and outside destructively
   rebuilt `build/`. Record process exit, native identity, SessionStart,
   unchanged files before restart confirmation, and observed limitations.
2. Add RED tests under `tests/ntgrill/`, then extend
   `src/runtime/{phase,nttask,ntgrill,artifacts}.ts` and `src/cli/{arguments,main}.ts` with
   ntgrill begin/stop/complete. Reuse strict state, ownership and transactions;
   keep existing nttask behavior. The resume smoke reproduced a missing
   same-owner `--interruption` instruction; the two nttask skills explicitly
   reacquire that owner after restart confirmation, matching existing runtime
   behavior. Test legal entry, artifact gates, explicit
   completion confirmation, interruption, blockers, locks, failed commits,
   and invalid later invocations with accurate next actions.
3. Add `plugins/{claude-code,codex}/skills/ntgrill/{SKILL.md,LICENSE}`; update
   relevant manifests and checked CLI bundles. Pin Matt Pocock's source to
   `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`. Neotolis explicitly adapts its
   rounds to one question and its scouting to the primary, as its contract
   requires. The skills remain self-contained and carry the MIT notice.
4. Extend provider component/discovery, conformance, E2E and packaging tests.
   Compare full normalized responses and filesystem trees; normalize only
   native identities and absolute fixture paths. Run affected suites after
   each increment, then sequential `npm run verify`. Release packaging must
   finish after the test that deletes `build/`.
5. Run separate live ntgrill with real models after the local deterministic
   gate. Review read-only with three agents: simplicity; architecture;
   correctness/security/test evidence. Fix confirmed findings and recheck.
6. Update `PROJECT.md` and this directory's `README.md` with current scope,
   evidence, source references, limitations and the need for new three-platform
   CI. Prior CI proves only the tracer commit, never these uncommitted changes.

The runtime can validate an explicit confirmation argument and BRIEF shape;
the primary skill owns the frontier sweep, truthful confirmation, and current
agreed meaning. Deterministic tests do not claim to prove model reasoning.
